package com.ualberta.team17.controller;

import java.util.Collection;

import com.ualberta.team17.datamanager.DataManager;
import com.ualberta.team17.datamanager.IItemComparator;
import com.ualberta.team17.datamanager.IItemFilter;

/*
 * Class that is responsible for pulling all of the information for a given search, and
 * notifying the view when it arrives.
 */
public class ThreadSearchController {
	public ThreadSearchController(DataManager source, Collection<IItemFilter> filter, IItemComparator sort) {
		
	}
	
	/* notify view when items arrive */
}
