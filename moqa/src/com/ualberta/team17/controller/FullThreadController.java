package com.ualberta.team17.controller;

import com.ualberta.team17.UniqueId;

/*
 * Class that is responsible for pulling all of the information for a given thread
 * from a given data source, and notifying the view when the parts arrive.
 */
public class FullThreadController {
	public FullThreadController(DataManager source, UniqueId questionId) {
		
	}
	
	/* Notifications back to the view that wants data in this format */
}
